spring:
  application:
    name: budget-service
  profiles:
    active: dev
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: true

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
