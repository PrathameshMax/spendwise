spring:
  application:
    name: gateway-service

server:
  port: 8088

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      probes:
        enabled: true