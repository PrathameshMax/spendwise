spring:
  application:
    name: auth-service
  profiles:
    active: dev
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: true

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
